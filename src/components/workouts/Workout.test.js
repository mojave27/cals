/** @jsx jsx */
import { jsx } from '@emotion/core'
import React from 'react'
import Workout from './Workout'

// import { mount, shallow, render } from 'enzyme'

describe('tests', () => {
  test('test', () => {
    // let wrapper = shallow(<Workout />)
    expect(true).toEqual(true)
  })
})